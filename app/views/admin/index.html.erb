<p style="color: green"><%= notice %></p>

<h1>Bookings</h1>

<div style="display:flex; align-center:true; margin:10px; flex-direction:column;" id="bookings">
  <% @bookings.each do |booking| %>
    <%= render booking %>
  <% end %>
</div>